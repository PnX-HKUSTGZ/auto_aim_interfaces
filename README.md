# auto_aim_interfaces

本包为自动瞄准系统（auto-aim system）提供统一的数据接口定义，包含 ROS 消息（msg）与服务（srv）类型，用于目标检测、识别、跟踪与控制模块之间的数据传递。

主要内容示例：
- 目标与识别相关消息（如 Armor、Target、Rune 等）
- 跟踪与状态信息（如 TrackerInfo）
- 控制/模式切换服务（如 SetMode.srv）

注意：本包仅包含接口（消息/服务）定义，不包含可独立运行的节点、算法或应用逻辑。必须与实现感知、规划或控制功能的其他包配合使用，不能单独作为可执行程序使用。
